Review the current session for non-obvious insights worth preserving. Look for:

- Debugging a tricky issue where the root cause was surprising
- A framework or library behavior that was unexpected
- An approach that failed before finding the right solution
- A UX/design correction from the user
- A code pattern the user preferred over what you initially produced
- A refactoring request that revealed a better abstraction

If you find something worth capturing, append a concise entry to `docs/learnings.md` under the most appropriate topic heading. Each entry should include:

1. **What happened** — one sentence describing the problem or surprise
2. **Why** — the root cause or explanation
3. **Takeaway** — the general lesson to apply in future sessions

If the insight has solidified into a hard rule (confirmed across multiple sessions), promote it to `CLAUDE.md` or `AGENTS.md` instead.

If nothing noteworthy happened this session, say so briefly and move on.
